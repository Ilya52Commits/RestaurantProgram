<Window x:Class="RestaurantProgram.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantProgram"
        xmlns:ViewModel ="clr-namespace:RestaurantProgram.ViewModel"
        mc:Ignorable="d"
        Title="Главное меню" Height="450" Width="800">
    <Window.DataContext>
        <ViewModel:MenuViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>

            
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            


        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Главное меню" HorizontalAlignment="Center" FontSize="35" />
        <ListBox ItemsSource="{Binding MenuNames}" 
                 HorizontalAlignment="Center" VerticalAlignment="Center" 
                 Width="300" Height="200" FontSize="20" 
                 Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding  }" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
